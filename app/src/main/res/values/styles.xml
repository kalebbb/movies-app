<resources>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="movie.theme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorAccent">@android:color/holo_green_dark</item>
    </style>

    <style name="search" parent="movie.theme">
        <item name="android:searchIcon">@drawable/ic_magnify_black_24dp</item>
        <item name="android:background">@color/whiteTransparence</item>
        <item name="android:editTextColor">@color/black</item>
        <item name="android:textColorHint">@color/grey</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:searchHintIcon">@drawable/ic_magnify_grey600_24dp</item>

    </style>

    <style name="cardMovie" parent="movie.theme">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="movie.theme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="movie.theme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
